const Header = () => {
    const view = `
    <div class="inner">
        <h3><a href="#">Guten Tag!</a></h3>
        <nav class="nav nav-masthead justify-content-center">
            <a class="nav-link active" href="#">Home</a>
            <a class="nav-link" href="#">About the developer</a>
        </nav>
    </div>
    `;
    return view;
};

export default Header;